{% extends 'layoutAdmin.html.twig' %}

{% block title %}Administration{% endblock %}

{% block body %}
    <section>
        <h1 class="title text-center mt-5">Administration</h1>

        <div class="container-fluid pb-4 text-center">
            <div class="row justify-content-around">
                <div class="card my-2 d-flex align-items-center">
                    <img class="icon card-img-top pt-3" src="{{ asset('build/images/ketchup.png') }}" alt="Logo administrateur">
                    <div class="card-body">
                        <h2 class="titreH2">Sauces</h2>
                        <a href="{{ path("admin_sauce_index") }}" class="btn btn-primary">Administrer</a>
                    </div>
                </div>

                <div class="card my-2 d-flex align-items-center">
                    <img class="icon card-img-top pt-3" src="{{ asset('build/images/comment.png') }}" alt="Logo administrateur">
                    <div class="card-body">
                        <h2 class="titreH2">Commentaires</h2>
                        <a href="{{ path('admin_comment_index') }}" class="btn btn-primary">Administrer</a>
                    </div>
                </div>


    </section>
{% endblock %}
